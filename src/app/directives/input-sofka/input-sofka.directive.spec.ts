import { InputSofkaDirective } from './input-sofka.directive';

describe('InputSofkaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSofkaDirective();
    expect(directive).toBeTruthy();
  });
});
