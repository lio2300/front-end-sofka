import { DropdownTemplateDirective } from './dropdown-template.directive';

describe('DropdownTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
